import { Loading } from '@/components/loading';
import type { Meta, Story } from '@storybook/react';

export default {
  title: 'Components/Loading',
  component: Loading,
} as Meta;

export const LoadingStory: Story = () => <Loading />;
